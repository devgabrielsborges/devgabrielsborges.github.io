{
  "name": "gabrielborges-portfolio",
  "version": "1.0.0",
  "type": "module",
  "description": "Gabriel Borges - Portfolio Website",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "update-projects": "node scripts/update-projects.js",
    "auto-deploy": "npm run update-projects && npm run deploy"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
